/**
 * demoList组件的locale - en
 * @author ronffy
 */
import getList from '../getList';

const code = 'en';
export default {
  code,
  title: 'Everything goes, everything will happen.',
  list: getList(code),
}